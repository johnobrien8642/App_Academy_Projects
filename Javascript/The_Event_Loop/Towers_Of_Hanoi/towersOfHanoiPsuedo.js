class TowersOfHanoi {
  run(){
    //check if all disks are on last pole

      //ask player for next move

        //determine if next move is valid
          //if it is valid, move disk to chosen pole
          //if it is invalid, as player for another move
  }
}